# Scene Control Language (SCL)
# Version 1.0

# Bootstrap Definitions

# System Commands
Register { Register, RegisterProcedure, 0, -1, "Registers a new command in the terminal."}
Register { Unregister, UnregisterProcedure, 1, 1, "Unregisters an existing command from the terminal."}

# Help Command
Register { Help, HelpProcedure, 0, -1, "Displays help information for all commands."}
